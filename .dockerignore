.git
.github
.githooks
.openai
.venv
.public-serving
dist
node_modules
pipeline
tests
test-results
playwright-report
package.json
package-lock.json
playwright.config.mjs
requirements.lock
README.md
CHART_SYSTEM.md
BRAND.md
PUBLIC_DOMAIN_DEPLOYMENT.md
brand-preview.html
serve.sh
*.log

# macOS "duplicate on copy" artefacts - "country-functions 2.js", "entities 3.json",
# "website 2/". Keeps ~25k stray duplicates out of the build context so `COPY .`
# can never publish them. Docker matches patterns against the whole context-relative
# path and `*` does not cross `/`, so each pattern is repeated under `**/`.
* [0-9]
* [0-9].*
* [0-9][0-9]
* [0-9][0-9].*
**/* [0-9]
**/* [0-9].*
**/* [0-9][0-9]
**/* [0-9][0-9].*
.warehouse-bundle

# Root-level markdown is project documentation, never site content. The named entries above
# predate this rule; it keeps every future planning note out of the image. `*` does not cross
# a slash, so the data/**/README.md files that document published datasets still ship.
*.md
